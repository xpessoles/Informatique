%\exer{Implémentation des graphes par une matrice d'adjacence d'un graphe pondéré}

\section*{Coloration d'un graphe}
\marginnote{D'après CCINP-TSI 2024.}

\subsection*{Introduction}

La coloration d'un graphe consiste à attribuer une couleur à chaque sommet.  Un $k$-coloriage donne une couleur (qui peut par exemple être un entier entre 0 et $k-1$, pour simplifier) à chaque sommet, tel que deux sommets adjacents soient de couleurs différentes. On cherche en général à minimiser le nombre de couleur utilisées. 


On peut appliquer ce problème au coloriage d'une carte de pays, ou d'un dessin d'ensemble (figure \label{12_coloration_fig_02}). 

Ainsi le coloriage de la pince (ainsi que le premier graphe) est un $k$-coloriage valide. Néanmoins, il ne minimise pas le nombre de couleurs utilisées. Le second graphe, quant à lui utilise un nomnbre minimal de couleurs. 

\begin{figure}[!h]
\includegraphics[width=\linewidth]{fig_02}
\caption{Coloriage de la pince Schrader \label{12_coloration_fig_02}}
\end{figure}


On modélise le graphe par un dictionnaire d'adjacence. Les clés sont les sommets, modélisés par des chaînes de caractères correspondant aux classes d'équivalences cinématiques. Les valeurs sont des listes de chaînes de caractères.

\question{Donner le dictionnaire d'adjacence \lstinline{Gex} associé au graphe de la figure \ref{12_coloration_fig_02}.}

\question{Implémenter la fonction \lstinline|voisins(G:{}, s1:str, s2:str) -> bool| renvoyant \lstinline{True} si \lstinline{s1} et \lstinline{s2} sont voisins, \lstinline{False} sinon.}

\question{Implémenter la fonction \lstinline|is_graphe_correct(G:{}) -> bool| renvoyant \lstinline{True} si lorsque \lstinline{s1} et \lstinline{s2} sont voisins alors \lstinline{s2} et \lstinline{s1} sont voisins, \lstinline{False} sinon.}

\question{Vérifier que \lstinline{Gex} est correctement implémenté. Le cas échéant, vérifier si le problème vient du graphe oiu de la fonction \lstinline|is_graphe_correct| .}

\question{Implémenter la fonction  \lstinline|liste_aretes(G:{}) -> [()]|  qui renvoie la liste des arêtes du graphe. Une arête sera modélisée par un couple (tuple) de deux sommets. Le graphe étant non orienté, si \lstinline{('A','B')} est dans la liste, \lstinline{('B','A')} ne devra pas y figurer.}

Une $k$-coloration est représentée par un dictionnaire \lstinline{C} pour lequel les clés sont les sommet des graphes et les valeurs sont des entiers (entre 0 et $k-1$) qui représentent la couleur du sommet.
Dans le cadre du graphe précédent, on a donc \lstinline|C = {'A':0, 'B':1, 'C':0, 'D':1, 'E':0, 'F':1}|.

On souhaite vérifier si une coloration est valide, c'est-à-dire que deux sommets du graphe reliés par une arête ne peuvent pas avoir la même couleur.

\question{Implémenter la fonction \lstinline|coloration_valide(G:{}, C:{}) -> bool| renvoyant \lstinline{True} si la coloration est valide, \lstinline{False} sinon. Tester votre fonction sur le dictionnaire de coloration précédent.}




\subsection*{Algorithme intuitif de coloration}

L’attribution des couleurs à chaque sommet est caractérisée par un dictionnaire \lstinline{C} où \lstinline{C[i]} est la couleur attribuée au sommet \lstinline{i}. \lstinline{C[i]} vaut \lstinline{-1} si la couleur n’est pas encore attribuée. Le dictionnaire \lstinline{C} ne contient initialement que des \lstinline{-1} et ses valeurs sont modifiées progressivement, au fur et à mesure que les couleurs sont attribuées.

\question{Ecrire une fonction \lstinline|init_c(G:{}) -> {}| permettant de créer un dictionnaire dont les clés sont identiques à celle du graphe. Ces cles sont associées à la valeur \lstinline{-1}.}

\begin{question}
Implémenter la fonction \lstinline|colore_sommet(G:{}, C:{}, s:str) -> None|. Cette fonction ne renvoie rien mais modifie le dictionnaire \lstinline{C} en donnant à \lstinline{C[s]} la plus petite couleur possible, en fonction des couleurs des sommets voisins qui sont déjà colorés.

Par exemple, pour le graphe \lstinline{Gex} , prenons \lstinline{C = ["A":0,"B":1,"C":-1,"D":-1,"E":-1,"F":-1]}. Les sommets \lstinline{"A"} et \lstinline{"B"} ont donc déjà été colorés avec les couleurs \lstinline{C["A"] = 0} et \lstinline{C["B"] = 1}. L’appel \lstinline{colore_sommet(G,C,"C")} modifie le dictionnaire \lstinline{C} en \lstinline{C = ["A":0,"B":1,"C":0,"D":-1,"E":-1,"F":-1]}. Cela veut dire que le sommet \lstinline{"C"}, adjacent au sommet \lstinline{"B"} mais pas au sommet \lstinline{"A"}, a reçu la même couleur que le sommet \lstinline{"A"}.
\end{question}

\begin{question}
Ecrire une fonction \lstinline{colorer1} avec pour argument un dictionnaire \lstinline{G} caractérisant un graphe, qui crée et renvoie le dictionnaire \lstinline{C} des numéros des couleurs attribuées en colorant les sommets un par un. 
\end{question}

\begin{question}
L’ordre de coloration imposé à la question précédente est arbitraire. On souhaite maintenant colorer le graphe en traitant les sommets selon un ordre arbitraire donné en argument. Écrire une fonction \lstinline{colorer2} analogue à \lstinline{colorer1} et avec un argument supplémentaire, une liste ordre fixant l’ordre de coloration des sommets.
\end{question}

\begin{question}
Donner la liste des couleurs renvoyée par \lstinline{colorer2} pour colorer le graphe \lstinline{Gex} donné en exemple en prenant \lstinline{ordre = ['E', 'F', 'B', 'D', 'A', 'C']}.

Combien de couleurs ont été utilisées ?
\end{question}


La méthode que nous venons de décrire est rapide et fonctionne plutôt bien. Cependant, si on cherche à utiliser le nombre minimum de couleurs, l’efficacité de l’algorithme proposé ci-dessus dépend en grande partie de l’ordre dans lequel on choisit de colorer les sommets du graphe. L’objectif des sous parties suivantes est d’affiner la stratégie pour mieux choisir cet ordre de coloration.

\subsection*{Variante de Welsh-Powell}
Une alternative est donnée par la variante de Welsh-Powell. L’idée est de parcourir l’ensemble des sommets du graphe par ordre décroissant de leurs degrés. Comme le degré d’un sommet est un entier positif, il est possible d’écrire un algorithme de tri efficace (dit par répartition).


\begin{question}
Écrire une fonction \lstinline{degre} avec pour argument le dictionnaire d’adjacence \lstinline{G} d’un graphe quelconque, qui renvoie la liste des degrés des sommets du graphe.
\end{question}

\begin{question}
Écrire une fonction \lstinline{init} avec pour argument un entier \lstinline{n}, qui renvoie une liste de listes \lstinline{R} de taille \lstinline{n}, telle que \lstinline{R[i]} soit une liste vide. Par exemple, \lstinline{init(3)} renverra \lstinline{[[],[],[]]}.
\end{question}

\begin{question}
Écrire une fonction \lstinline{ranger} avec pour argument un dictionnaire d’adjacence \lstinline{G}, qui renvoie une liste \lstinline{R} de même taille que \lstinline{G}, telle que \lstinline{R[i]} soit la liste des sommets de degré \lstinline{i}.
\end{question}


\begin{question}
Écrire une fonction \lstinline{renverse} avec pour argument une liste \lstinline{L}, qui crée et renvoie une nouvelle liste obtenue en lisant \lstinline{L} dans l’ordre inverse. Par exemple, \lstinline{renverse([1,2,3,4])} renverra \lstinline{[4,3,2,1]}.
\end{question}


\begin{question}
Écrire une fonction \lstinline{trier_sommets} avec pour argument un dictionnaire d’adjacence \lstinline{G}, qui renvoie la liste des sommets triés dans l’ordre décroissant de leur degré. 

%Par exemple, pour un graphe de liste d’adjacence LA = [[1,2],[0,2,3],[0,1,3],[1,2,4],[3]], la fonction trier_sommets renverra la liste des ommets [1,2,3,0,4].
\end{question}


\begin{question}
Pour un graphe à n sommets, quelle est la complexité temporelle de la fonction \lstinline{trier_sommets}
dans le pire des cas ?
\end{question}


\begin{question}
Écrire la fonction \lstinline{colorer3} avec pour argument un dictionnaire d’adjacence \lstinline{G}, qui crée et renvoie un dictionnaire de couleurs \lstinline{C}, telle que \lstinline{C[i]} soit la couleur à attribuer au sommet \lstinline{i}, les sommets étant colorés dans l’ordre décroissant de leur degré.

Quelle est la complexité de \lstinline{colorer3} dans le pire des cas pour un graphe à \lstinline{n} sommets ?
\end{question}


\begin{question}
Pour le graphe \lstinline{Gex}, donner la liste \lstinline{C} des couleurs renvoyée par la fonction
\lstinline{colorer3}.
\end{question}


L’amélioration proposée donne de bons résultats dans un grand nombre de cas. Cependant, il reste
quelques cas où la coloration obtenue utilise trop de couleurs.

%La méthode suivante, proposée en 1979 par Danier Brélaz de l’École Polytechnique Fédérale de
%Lausanne, raffine la détermination de l’ordre de coloration. La priorité de coloration est ainsi recalculée
%après chaque traitement d’un sommet et non plus une fois pour toute au départ. Au final, cette
%approche fournit rapidement une coloration optimale dans un très grand nombre de cas.