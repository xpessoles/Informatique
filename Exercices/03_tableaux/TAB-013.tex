\question \'Ecrire une fonction \pyv{miroir(t)} qui, à un tableau \pyv{t} de nombres, renvoie le tableau contenant les même nombres, renversé.

\begin{exemple}
  Avec  \pyv{t = [5,2,6,3,7]}, \pyv{miroir(t)} renverra \pyv{[7,3,6,2,5]}.
\end{exemple}