 
\question{Écrire une fonction \pyv{maxi_double(M)} prenant en argument une matrice de nombres \pyv{M} (représentée comme liste de listes) et renvoyant le maximum de \pyv{M}?}
%\end{exo}


\question{\'Ecrire une fonction \pyv{liste_imax(t)} renvoyant la liste des indices où le maximum de \pyv{t} est atteint.}