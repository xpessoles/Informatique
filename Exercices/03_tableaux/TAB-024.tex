\exer{}
\setcounter{numques}{0}

 
\question{Écrire une fonction \texttt{maxi\_double(M)} prenant en argument une matrice de nombres \texttt{M} (représentée comme liste de listes) et renvoyant le maximum de \texttt{M}?}
%\end{exo}


\question{\'Ecrire une fonction \texttt{liste\_imax(t)} renvoyant la liste des indices où le maximum de \texttt{t} est atteint.}