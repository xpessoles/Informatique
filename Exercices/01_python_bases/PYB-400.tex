
\exer{}
\setcounter{numques}{0}

\question {Indenter de deux manières différentes la suite d'instructions suivante afin que la variable \texttt{t} contienne \texttt{True} pour une indentation, puis \texttt{False} pour l'autre. }
%Le tester avec IDLE.
\begin{lstlisting}
x = 0
y = 5
t = False
if x>=1:
t = True
if y <= 6:
t = True
\end{lstlisting}
