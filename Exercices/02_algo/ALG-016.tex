\exer{}
\setcounter{numques}{0}

Pour les deux questions suivantes, les fonctions \texttt{max} et \texttt{min} ne sont pas autorisées.

\question{Écrire une fonction \texttt{chercheMax(L)} qui prend comme argument une liste \texttt{L} d'entiers \texttt{int} et qui renvoie le plus grand élément de la liste.}

\question{Écrire une fonction \texttt{chercheMaxIndice(L)} qui prend comme argument une liste \texttt{L} d'entiers \texttt{int} et qui renvoie l'indice du plus grand élément de la liste.}