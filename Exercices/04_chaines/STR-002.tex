\exer{}
\setcounter{numques}{0}

\question{ \'Ecrire une fonction \texttt{c\_recherche(m,s)} de recherche indiquant si le mot \texttt{m} est dans la chaîne \texttt{s} insensible à la casse (majuscules ou minuscules).}

\question{ \'Ecrire une fonction \texttt{i\_recherche(m,s)} de recherche du mot \texttt{m} dans une chaîne \texttt{s}, renvoyant la position de la première occurrence du mot dans la chaîne et une erreur si le mot n'est pas dans le texte.}