\question{ \'Ecrire une fonction \pyv{c_recherche(m,s)} de recherche indiquant si le mot \pyv{m} est dans la chaîne \pyv{s} insensible à la casse (majuscules ou minuscules).}

\question{ \'Ecrire une fonction \pyv{i_recherche(m,s)} de recherche du mot \pyv{m} dans une chaîne \pyv{s}, renvoyant la position de la première occurence du mot dans la chaîne et une erreur si le mot n'est pas dans le texte.}